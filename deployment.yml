apiVersion: v1
kind: Service
metadata:
  name: capstone

spec:
  selector:
    app: capstone
  ports:
    - port: 80
      protocol: TCP
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone

spec:
  replicas: 1
  selector:
    matchLabels:
      app: capstone
  template:
    metadata:
      labels:
        app: capstone
    spec:
      containers:
        - name: capstone
          image: 326983632510.dkr.ecr.us-east-1.amazonaws.com/capstone-app:latest
          imagePullPolicy: "Always"
          ports:
            - containerPort: 80
          resources: {}